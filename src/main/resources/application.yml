info:
  app:
    name: Flower Store
    description: This is a webstore app for iti0203.

server:
  port: 8080
  servlet:
    context-path: /api

spring:
  profiles:
    active: "dev"
  jackson:
    default-property-inclusion: non_null
  jpa:
    open-in-view: false
    database: POSTGRESQL
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        current_session_context_class: thread
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true
  datasource:
    url: jdbc:postgresql://${DATABASE_CONTAINER_NAME}:5432/webstoredb
    username: postgres
    password: fudd386a61h2sdsbn3bu3bi37873bdabd3b73ada56yxvnm4y737ihsgf
    platform: postgres
  h2:
    console:
      enabled: true

logging:
  config: classpath:logback-spring.xml
  level:
    ROOT: INFO

---

spring:
  profiles: dev
logging:
  path: .
  file: logs/dev

---

spring:
  profiles: prod
logging:
  path: /logs/back
  file: prod

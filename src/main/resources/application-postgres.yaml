server:
  port: 8080
  servlet:
    context-path: /api
spring:
  datasource:
    url: jdbc:postgresql://localhost:5632/webstoredb
    username: postgres
    password: sakv41basvbwoiv45yhwrcbo
    platform: postgres
  jackson:
    default-property-inclusion: non_null
  jpa:
    open-in-view: false
    database: POSTGRESQL
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        current_session_context_class: thread
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true
logging:
  path: logs
  file: webstore
  config: classpath:logback-spring.xml
  level:
    ROOT: INFO
